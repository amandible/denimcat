# Copy this file to .env.local for local overrides, or set it as a Vercel
# environment variable for the deployed build.
# Defaults to http://localhost:4000 if unset.
VITE_SERVER_URL=https://your-server.fly.dev
